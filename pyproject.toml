# AI assisted development
[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agentic-triage-platform"
version = "0.1.0"
description = "Agentic triage platform"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["app*"]
